import { FormEvent, useState } from 'react';
import { LogOut, PackagePlus, RefreshCw, ShieldCheck, Trash2 } from 'lucide-react';

type Product = { id: string; title: string; description: string; price: number; location: string; category: string; imageUrl: string; status: 'available' | 'reserved' | 'sold' | 'hidden' };
type Order = { id: string; status: 'pending' | 'paid' | 'cancelled'; customer: { name: string; email: string; phone?: string }; productIds: string[]; createdAt: string };
const emptyProduct = { title: '', description: '', price: '', location: '', category: 'Local commercial', imageUrl: '', status: 'available' as Product['status'] };

async function adminFetch(path: string, token: string, options?: RequestInit) {
  const response = await fetch(path, { ...options, headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}`, ...(options?.headers ?? {}) } });
  if (!response.ok) throw new Error((await response.json()).error ?? 'Request failed');
  return response.status === 204 ? null : response.json();
}

export default function AdminDashboard() {
  const [token, setToken] = useState('');
  const [authenticated, setAuthenticated] = useState(false);
  const [products, setProducts] = useState<Product[]>([]);
  const [orders, setOrders] = useState<Order[]>([]);
  const [form, setForm] = useState(emptyProduct);
  const [error, setError] = useState('');
  const [loading, setLoading] = useState(false);

  const load = async () => {
    setLoading(true); setError('');
    try { const [nextProducts, nextOrders] = await Promise.all([adminFetch('/api/admin/products', token), adminFetch('/api/admin/orders', token)]); setProducts(nextProducts); setOrders(nextOrders); setAuthenticated(true); }
    catch (err) { setError(err instanceof Error ? err.message : 'Accès refusé'); setAuthenticated(false); }
    finally { setLoading(false); }
  };
  const submit = async (event: FormEvent) => { event.preventDefault(); try { await adminFetch('/api/admin/products', token, { method: 'POST', body: JSON.stringify({ ...form, price: Number(form.price) }) }); setForm(emptyProduct); await load(); } catch (err) { setError(err instanceof Error ? err.message : 'Impossible de créer le bien'); } };
  const remove = async (id: string) => { await adminFetch(`/api/admin/products?id=${encodeURIComponent(id)}`, token, { method: 'DELETE' }); await load(); };

  if (!authenticated) return <main className="flex min-h-screen items-center justify-center bg-brand-dark px-6 text-white"><form onSubmit={(event) => { event.preventDefault(); load(); }} className="w-full max-w-md rounded-2xl border border-white/10 bg-[#121a29] p-8 shadow-2xl"><div className="mb-8 flex items-center gap-3"><ShieldCheck className="text-gold-400" /><div><p className="text-xs uppercase tracking-[0.25em] text-gold-400">Northline Commercial</p><h1 className="mt-1 font-display text-2xl font-bold">Accès administration</h1></div></div><label className="block text-sm text-gray-300" htmlFor="admin-token">Clé d&apos;accès Admin</label><input id="admin-token" type="password" value={token} onChange={(event) => setToken(event.target.value)} className="mt-2 w-full rounded-lg border border-white/10 bg-black/20 px-4 py-3 outline-none focus:border-gold-400" autoComplete="current-password" required /><button className="mt-5 w-full rounded-lg bg-gold-400 px-4 py-3 font-bold text-brand-dark" disabled={loading}>{loading ? 'Vérification…' : 'Ouvrir le back-office'}</button>{error && <p role="alert" className="mt-4 text-sm text-red-300">{error}</p>}</form></main>;

  return <main className="min-h-screen bg-brand-dark px-4 py-8 text-white sm:px-8"><div className="mx-auto max-w-7xl"><header className="mb-8 flex flex-col justify-between gap-4 border-b border-white/10 pb-6 sm:flex-row sm:items-center"><div><p className="text-xs uppercase tracking-[0.25em] text-gold-400">Northline Commercial</p><h1 className="mt-2 font-display text-3xl font-bold">Back-office catalogue</h1></div><div className="flex gap-3"><button onClick={load} className="flex items-center gap-2 rounded-lg border border-white/15 px-4 py-2 text-sm hover:border-gold-400"><RefreshCw className="h-4 w-4" /> Actualiser</button><button onClick={() => { setAuthenticated(false); setToken(''); }} className="flex items-center gap-2 rounded-lg border border-white/15 px-4 py-2 text-sm hover:border-red-400"><LogOut className="h-4 w-4" /> Quitter</button></div></header><div className="grid gap-8 lg:grid-cols-[380px_1fr]"><form onSubmit={submit} className="h-fit rounded-2xl border border-white/10 bg-[#121a29] p-6"><div className="mb-5 flex items-center gap-2"><PackagePlus className="text-gold-400" /><h2 className="font-display text-xl font-bold">Nouveau bien</h2></div><div className="flex flex-col gap-3">{[['title','Titre'],['description','Description'],['location','Localisation'],['imageUrl','URL image']].map(([key, label]) => <label key={key} className="text-sm text-gray-300">{label}<input value={form[key as keyof typeof form]} onChange={(event) => setForm({ ...form, [key]: event.target.value })} className="mt-1 w-full rounded-lg border border-white/10 bg-black/20 px-3 py-2 text-white outline-none focus:border-gold-400" required={key !== 'description' && key !== 'imageUrl'} /></label>)}<label className="text-sm text-gray-300">Prix<input type="number" min="0" value={form.price} onChange={(event) => setForm({ ...form, price: event.target.value })} className="mt-1 w-full rounded-lg border border-white/10 bg-black/20 px-3 py-2 outline-none focus:border-gold-400" required /></label><label className="text-sm text-gray-300">Catégorie<select value={form.category} onChange={(event) => setForm({ ...form, category: event.target.value })} className="mt-1 w-full rounded-lg border border-white/10 bg-black/20 px-3 py-2"><option>Local commercial</option><option>Terrain</option><option>Appartement</option><option>Véhicule</option></select></label><label className="text-sm text-gray-300">Statut<select value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value as Product['status'] })} className="mt-1 w-full rounded-lg border border-white/10 bg-black/20 px-3 py-2"><option value="available">Disponible</option><option value="reserved">Réservé</option><option value="sold">Vendu</option><option value="hidden">Masqué</option></select></label><button className="mt-2 rounded-lg bg-gold-400 px-4 py-3 font-bold text-brand-dark">Ajouter le bien</button></div></form><section className="flex flex-col gap-8"><div className="overflow-hidden rounded-2xl border border-white/10 bg-[#121a29]"><div className="border-b border-white/10 p-5"><h2 className="font-display text-xl font-bold">Catalogue ({products.length})</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-white/5 text-xs uppercase tracking-wider text-gray-400"><tr><th className="p-4">Bien</th><th className="p-4">Localisation</th><th className="p-4">Prix</th><th className="p-4">Statut</th><th className="p-4">Action</th></tr></thead><tbody>{products.map((product) => <tr key={product.id} className="border-t border-white/5"><td className="p-4 font-semibold">{product.title}</td><td className="p-4 text-gray-400">{product.location}</td><td className="p-4">{product.price.toLocaleString('fr-FR')}</td><td className="p-4"><span className="rounded-full bg-white/10 px-3 py-1 text-xs">{product.status}</span></td><td className="p-4"><button aria-label={`Masquer ${product.title}`} onClick={() => remove(product.id)} className="text-gray-400 hover:text-red-300"><Trash2 className="h-4 w-4" /></button></td></tr>)}</tbody></table>{products.length === 0 && <p className="p-8 text-center text-gray-500">Aucun bien administrable.</p>}</div></div><div className="overflow-hidden rounded-2xl border border-white/10 bg-[#121a29]"><div className="border-b border-white/10 p-5"><h2 className="font-display text-xl font-bold">Réservations récentes ({orders.length})</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-white/5 text-xs uppercase tracking-wider text-gray-400"><tr><th className="p-4">Client</th><th className="p-4">Biens réservés</th><th className="p-4">Statut</th><th className="p-4">Date</th></tr></thead><tbody>{orders.map((order) => <tr key={order.id} className="border-t border-white/5"><td className="p-4"><strong>{order.customer.name}</strong><br /><span className="text-gray-400">{order.customer.email}</span></td><td className="p-4 text-gray-400">{order.productIds.join(', ')}</td><td className="p-4"><span className="rounded-full bg-white/10 px-3 py-1 text-xs">{order.status}</span></td><td className="p-4 text-gray-400">{new Date(order.createdAt).toLocaleDateString('fr-FR')}</td></tr>)}</tbody></table>{orders.length === 0 && <p className="p-8 text-center text-gray-500">Aucune réservation récente.</p>}</div></div></section></div></div></main>;
}
